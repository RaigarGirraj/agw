// import { isFunctionDeclaration } from "typescript";

// document.querySelector('.about-EE').style.transform = `translateX(151%)`;
// window.addEventListener('scroll', () => {
//     let y = window.scrollY;
//     let p = y*2.5+'px';
//     let p2 = (y)*(-2.5) + 2000;
//     document.querySelector('.about-EE').style.transform = `translateX(${(p2 + 'px')})`;
// });

// ScrollReveal().reveal('.about-IITK', { delay: 200, origin: 'left', distance: '100px' });
window.sr = ScrollReveal({ reset: true });

// Custom Settings
sr.reveal('.about-IITK', { duration: 200 });
